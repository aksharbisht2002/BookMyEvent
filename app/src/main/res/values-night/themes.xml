<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Evento" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Customize your dark theme here. -->
        <item name="android:statusBarColor">@color/dark_grey</item>
        <item name="colorPrimary">@color/dark_grey</item>
    </style>

    <style name="Theme.Evento" parent="Base.Theme.Evento" >
dar
    </style>
    <style name="AppTheme" parent="Base.Theme.Evento">
        <item name="android:windowContentTransitions">true</item>
        <item name="buttonStyle">@color/black</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:progressTint">@color/white</item>
    </style>

</resources>